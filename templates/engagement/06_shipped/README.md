# 06_shipped/

Closed engagements live here permanently. The document is append-only history — evidence that further affirms or strengthens the engagement's findings may be added; existing content is never edited or removed.

**Entry:** §10 findings written AND §11 scorecard updated AND §13 closeout complete → `git mv` from `../05_evidence/`.

**Exit:** none. Shipped engagements are permanent record.

The `scorecard-consistency-check` action reads every engagement here to compute the current scorecard state. Every scorecard cell traces back to at least one engagement in this folder (or its absence, which means the cell is at L0/L1).

For eos-integrated targets, engagements here that falsified prior eos attestations have already had their backlog cycles auto-filed by the `eos-backlog-auto-file` action.
