# 03_ready/

Engagements here are fully authored, approved, and ready to execute. The approver has signed §5. The attack matrix (§7), oracles (§8), and target model (§6) are locked. Execution has not yet begun.

**Entry:** approver signs §5 AND §6-§8 complete → `git mv` from `../02_planning/`.

**Exit criterion:** agent begins executing attacks → `git mv` to `../04_running/`.

Engagements may stay in this stage indefinitely waiting for a scheduled window. Once an engagement moves to `04_running/`, the authorization clock (§2 `expiresAt`) starts consuming.
