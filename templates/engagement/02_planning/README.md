# 02_planning/

Engagements here are being decomposed by an agent. The target model (§6), the attack matrix (§7), and the oracles (§8) are being authored. The document evolves as the agent enumerates specific attacks against specific claims.

**Entry:** agent begins decomposition → `git mv` from `../01_authorized/`.

**Exit criterion:** §6-§8 populated AND approver signs §5 → `git mv` to `../03_ready/`.

This is the collaborative phase — the operator/approver reviews the attack matrix and either approves it or requests scope adjustments before the engagement can execute.
