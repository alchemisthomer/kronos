{
  "name": "@kronos/salesforce-reverse-data-loader",
  "version": "0.1.0",
  "description": "Kronos tool: audit what a Salesforce user can access and (only when explicitly instructed) extract that data as CSV. The reverse of Data Loader — reproduces what a compromised or over-privileged user could pull.",
  "type": "module",
  "private": true,
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "describe": "tsx src/index.ts --describe"
  },
  "dependencies": {
    "jsforce": "^3.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
