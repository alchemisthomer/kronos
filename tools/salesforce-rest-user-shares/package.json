{
  "name": "@kronos/salesforce-rest-user-shares",
  "version": "0.1.0",
  "description": "Kronos tool: enumerate the sharing grants that give one Salesforce user access to records beyond their explicit Object/Field permissions — role hierarchy, group memberships, per-sobject *Share rows.",
  "type": "module",
  "private": true,
  "license": "AGPL-3.0-only",
  "engines": { "node": ">=22.0.0" },
  "scripts": {
    "start": "tsx src/index.ts",
    "describe": "tsx src/index.ts --describe"
  },
  "dependencies": { "jsforce": "^3.7.0" },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
